
#ifndef JSK_JSK_H_
#define JSK_JSK_H_

#include <utils/types.h>
//#include <jet/jet.h>
//#include <jsk/types.h>
#include <jsk/config.h>




//jerr_t jsk_socket (int domain, int protocol, jsk_t *sk);
//jerr_t jsk_close (jsk_t s);
//
//jerr_t jsk_setsockopt (jsk_t s, int level, int option, const void *optval, size_t optvallen);
//jerr_t jsk_getsockopt (jsk_t s, int level, int option, void *optval, size_t *optvallen);
//
//jerr_t jsk_bind (jsk_t s, const char *addr);
//jerr_t jsk_connect (jsk_t s, const char *addr);
//jerr_t jsk_socketpair (jsk_t s1, jsk_t s2);
//jerr_t jsk_shutdown (jsk_t s, int how);
//
//int jsk_send (jsk_t s, const jbuffer_t *buf, st_flag_t flags, jtime_t timeout);
//int jsk_recv (jsk_t s, jbuffer_t **buf, st_flag_t flags, jtime_t timeout);

//int jsk_sendmsg (jsk_t s, const struct msghdr *msghdr, int flags);
//int jsk_recvmsg (int s, struct msghdr *msghdr, int flags);


#endif /* JSK_JSK_H_ */
